import { Results, SubNav } from '../components';
import { BaseLayout } from '../layouts';

const HomePage = () => {
  return (
    <BaseLayout>
      <SubNav />
      <Results />
    </BaseLayout>
  )
}

export default HomePage
